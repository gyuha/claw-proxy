---
phase: 01-core-proxy-foundation
plan: 03
type: execute
wave: 3
depends_on:
  - 01-01
  - 01-02
files_modified:
  - core/src/lib.rs
  - core/src/providers/mod.rs
  - core/src/providers/openai.rs
  - core/tests/openai_provider.rs
autonomous: false
requirements:
  - PROXY-04
  - PROV-01
must_haves:
  truths:
    - "The OpenAI adapter can be exercised against a deterministic mock upstream instead of the public network."
    - "Successful upstream chat completions become canonical `InternalResponse` values with typed usage fields."
    - "Upstream OpenAI failures are returned as sanitized proxy errors that do not leak raw upstream bodies or secrets."
    - "A manual smoke run can validate real OpenAI traffic plus Claude Code local gateway use against the finished proxy with only `ANTHROPIC_BASE_URL` changed."
  artifacts:
    - path: core/src/providers/openai.rs
      provides: "Injectable, typed OpenAI adapter"
    - path: core/src/lib.rs
      provides: "Production provider construction with the default OpenAI base URL"
    - path: core/tests/openai_provider.rs
      provides: "Mock-upstream OpenAI adapter coverage"
  key_links:
    - from: core/src/lib.rs
      to: core/src/providers/openai.rs
      via: "build_providers constructs the production OpenAI adapter"
      pattern: "OpenAIProvider::new"
    - from: core/tests/openai_provider.rs
      to: core/src/providers/openai.rs
      via: "constructor-time base URL injection"
      pattern: "new_with_base_url|base_url"
---

<objective>
Finish the first real provider path by making the OpenAI adapter deterministic under test, typed in its response parsing, and safe in its upstream error handling.

Purpose: Satisfy PROV-01 without leaking provider details and finish the Phase 1 verification loop with a human smoke check.
Output: Mock-upstream adapter tests, injectable OpenAI base URL seam, and a final manual verification task that runs after the Anthropic gateway and aliasing work from Plan 02.
</objective>

<execution_context>
@/Users/gyuha/workspace/claw-proxy-worktrees/feat-gsd-01/.codex/get-shit-done/workflows/execute-plan.md
@/Users/gyuha/workspace/claw-proxy-worktrees/feat-gsd-01/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/01-core-proxy-foundation/01-RESEARCH.md
@.planning/phases/01-core-proxy-foundation/01-VALIDATION.md
@AGENTS.md
@core/src/lib.rs
@core/src/providers/mod.rs
@core/src/providers/openai.rs
@core/src/normalizer/mod.rs

<interfaces>
From `core/src/providers/mod.rs`:
```rust
#[async_trait]
pub trait Provider: Send + Sync {
    fn name(&self) -> &str;
    fn is_available(&self) -> bool;
    fn supports_model(&self, model: &str) -> bool;
    async fn chat_completion(
        &self,
        request: InternalRequest,
    ) -> Result<InternalResponse, AppError>;
}
```

From `core/src/lib.rs`:
```rust
pub fn build_providers(config: &Config) -> Vec<Box<dyn Provider>>;
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add mock-upstream coverage for OpenAI success and sanitized failure paths</name>
  <files>core/tests/openai_provider.rs</files>
  <read_first>.planning/phases/01-core-proxy-foundation/01-VALIDATION.md, .planning/phases/01-core-proxy-foundation/01-RESEARCH.md, core/src/providers/openai.rs, core/src/providers/mod.rs, core/src/normalizer/mod.rs</read_first>
  <action>Create `core/tests/openai_provider.rs` with an exact test named `openai_provider_mock_upstream` that starts a local Axum mock upstream, constructs the adapter against that base URL, sends a canonical text-only `InternalRequest`, and asserts both success mapping (`id`, `model`, `content`, `usage.prompt_tokens`, `usage.completion_tokens`) and sanitized failure mapping for non-2xx upstream responses. Use fixture responses that include a raw upstream error body so the test can prove the adapter does not surface it verbatim.</action>
  <acceptance_criteria>
    - `rg -n "openai_provider_mock_upstream" core/tests/openai_provider.rs` finds the exact validation test name
    - `rg -n "Axum|Router|route" core/tests/openai_provider.rs` shows the test uses a local mock upstream rather than the public network
    - `rg -n "does not contain|not contains|assert!\\(!.*raw" core/tests/openai_provider.rs` or equivalent assertion proves sanitized failure behavior is tested
  </acceptance_criteria>
  <verify>
    <automated>cargo test -p claw-proxy-core openai_provider_mock_upstream -- --exact</automated>
  </verify>
  <done>Adapter coverage exists for deterministic success and failure behavior without requiring a real OpenAI credential.</done>
</task>

<task type="auto">
  <name>Task 2: Make the OpenAI adapter injectable, typed, and sanitized</name>
  <files>core/src/lib.rs, core/src/providers/mod.rs, core/src/providers/openai.rs</files>
  <read_first>.planning/REQUIREMENTS.md, .planning/phases/01-core-proxy-foundation/01-RESEARCH.md, core/src/lib.rs, core/src/providers/mod.rs, core/src/providers/openai.rs, core/src/normalizer/mod.rs</read_first>
  <action>Refactor `core/src/providers/openai.rs` so the adapter owns a constructor-time `base_url` seam, such as `OpenAIProvider::new_with_base_url(name, api_key, models, base_url)`, while `OpenAIProvider::new(...)` continues to target `https://api.openai.com`. Replace the ad-hoc `serde_json::Value` parsing with typed response structs for `id`, `model`, `choices[0].message.content`, and `usage.{prompt_tokens,completion_tokens}`. When the upstream returns a non-success status, return a sanitized `AppError::Provider` that includes the status code but not the raw upstream body or bearer token. Keep `core/src/lib.rs::build_providers` on the default constructor so production traffic still uses the real OpenAI API without any router changes.</action>
  <acceptance_criteria>
    - `rg -n "new_with_base_url|base_url" core/src/providers/openai.rs` shows the constructor-time mock seam exists
    - `rg -n "struct OpenAI.*Response|prompt_tokens|completion_tokens" core/src/providers/openai.rs` shows typed upstream decoding replaced ad-hoc JSON indexing
    - `rg -n "https://api\\.openai\\.com" core/src/providers/openai.rs core/src/lib.rs` shows the default production path remains OpenAI-only
    - `cargo test -p claw-proxy-core openai_provider_mock_upstream -- --exact` passes
  </acceptance_criteria>
  <verify>
    <automated>cargo test -p claw-proxy-core openai_provider_mock_upstream -- --exact</automated>
  </verify>
  <done>The only real provider in Phase 1 is testable with a mock upstream and no longer leaks raw upstream failure payloads.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Run the final local smoke checks for real OpenAI traffic and Claude Code gateway usage</name>
  <files>README.md, configs/config.example.yaml</files>
  <read_first>.planning/phases/01-core-proxy-foundation/01-VALIDATION.md, README.md, configs/config.example.yaml, .planning/phases/01-core-proxy-foundation/01-01-PLAN.md, .planning/phases/01-core-proxy-foundation/01-02-PLAN.md</read_first>
  <what-built>Completed Phase 1 automation: standalone boot, strict OpenAI/Anthropic ingress handling, `/v1/messages/count_tokens`, server-side Anthropic model aliasing, and the mock-tested OpenAI provider path.</what-built>
  <action>After Task 2 is green, perform the two manual validations already defined in `01-VALIDATION.md`. First, copy `configs/config.example.yaml` to a local temp file, replace `sk-YOUR_KEY_HERE` with a real OpenAI key, start the proxy with `cargo run -p claw-proxy-core -- <temp-config>`, and send a real `curl` request to `http://127.0.0.1:<proxy_port>/v1/chat/completions`. Second, export only `ANTHROPIC_BASE_URL=http://127.0.0.1:<proxy_port>`, leave `ANTHROPIC_MODEL`, `ANTHROPIC_SMALL_FAST_MODEL`, and similar model overrides unset, run one simple Claude Code prompt against the proxy, and confirm the proxy receives both `/v1/messages` and `/v1/messages/count_tokens` traffic without any Claude-specific client patching.</action>
  <how-to-verify>1. Start the proxy with a temp config that contains a real OpenAI key. 2. Run the README `curl` request against `/v1/chat/completions` and confirm HTTP 200 plus `"object":"chat.completion"`. 3. Export only `ANTHROPIC_BASE_URL=http://127.0.0.1:&lt;proxy_port&gt;` and ensure Anthropic model override env vars are unset. 4. Run one Claude Code prompt. 5. Confirm the proxy handles both `/v1/messages` and `/v1/messages/count_tokens` during that session.</how-to-verify>
  <acceptance_criteria>
    - The OpenAI smoke request returns HTTP 200 and includes `"object":"chat.completion"` in the JSON body
    - The Claude Code smoke run completes one text prompt with only `ANTHROPIC_BASE_URL` changed and no Anthropic model override env vars set
    - Proxy logs or captured requests show both `/v1/messages` and `/v1/messages/count_tokens` during the Claude Code run
  </acceptance_criteria>
  <verify>
    <automated>cargo test -p claw-proxy-core openai_provider_mock_upstream -- --exact && cargo test -p claw-proxy-core anthropic_messages_roundtrip -- --exact && cargo test -p claw-proxy-core anthropic_gateway_contract -- --exact</automated>
    <manual>1. Start the proxy with a temp YAML config containing a real OpenAI key. 2. Run the documented `curl` smoke request against `/v1/chat/completions`. 3. Export only `ANTHROPIC_BASE_URL=http://127.0.0.1:&lt;proxy_port&gt;` and ensure Anthropic model override env vars are unset. 4. Run one Claude Code prompt and confirm `/v1/messages` plus `/v1/messages/count_tokens` traffic hit the proxy. 5. Resume with `approved` or a defect list.</manual>
  </verify>
  <resume-signal>Resume with `approved` if both manual checks pass, or provide the failing request/response details.</resume-signal>
  <done>Manual smoke evidence confirms the finished Phase 1 proxy works with both a real OpenAI request and a Claude Code gateway session.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| proxy adapter → OpenAI upstream | Outbound HTTP leaves the local proxy and crosses into an external provider |
| test harness → mock upstream | Deterministic tests substitute a local server for the real provider |
| manual operator → real API key config | Human-supplied secrets enter the local smoke workflow |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-01 | I | `core/src/providers/openai.rs` upstream failure path | mitigate | Sanitize non-2xx provider errors to include status code only and cover the absence of raw upstream body leakage in `openai_provider_mock_upstream` |
| T-03-02 | T | constructor-time base URL seam | mitigate | Limit alternate base URL usage to explicit constructor injection in tests; keep `build_providers` on the default OpenAI constructor for production |
| T-03-03 | I | manual smoke workflow | mitigate | Use a temp local config for the real key, avoid committing it, and keep runtime logs free of bearer tokens or raw upstream error payloads |
| T-03-04 | E | local-only MVP exposure | accept | The proxy still binds to localhost in Phase 1; remote/admin hardening is deferred to later work by project scope |
</threat_model>

<verification>
Run `cargo test -p claw-proxy-core openai_provider_mock_upstream -- --exact` before the manual smoke task, then complete the OpenAI and Claude Code checks from `01-VALIDATION.md`.
</verification>

<success_criteria>
- The OpenAI provider is validated against a local mock upstream instead of live internet dependencies.
- Upstream failures are sanitized and do not expose raw response bodies or secrets.
- Manual smoke confirms a real OpenAI request and a Claude Code session both work against the local proxy with only `ANTHROPIC_BASE_URL` changed.
</success_criteria>

<output>
After completion, create `.planning/phases/01-core-proxy-foundation/01-03-SUMMARY.md`
</output>
