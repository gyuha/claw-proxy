---
phase: 01-runtime-foundation
plan: 03
title: Build the minimal control surface on top of runtime contracts
type: execute
wave: 3
depends_on:
  - "02"
files_modified:
  - src/app/App.tsx
  - src/components/shell/AppShell.tsx
  - src/components/status/RuntimeStatusCard.tsx
  - src/features/runtime/api.ts
  - src/features/runtime/state.ts
  - src/features/runtime/__tests__/runtime-status.test.tsx
  - src/styles/app.css
autonomous: true
requirements:
  - DESK-02
must_haves:
  truths:
    - "User can open Claw Proxy and see a structured desktop control surface"
    - "The UI renders real runtime snapshot data instead of local placeholder truth"
    - "Future settings areas are visible without implying unfinished provider features already exist"
  artifacts:
    - path: "src/components/shell/AppShell.tsx"
      provides: "Main control-plane layout"
    - path: "src/components/status/RuntimeStatusCard.tsx"
      provides: "Runtime status view bound to snapshot data"
    - path: "src/features/runtime/state.ts"
      provides: "UI state wrapper over typed IPC helpers"
    - path: "src/features/runtime/__tests__/runtime-status.test.tsx"
      provides: "Status rendering verification"
  key_links:
    - from: "src/features/runtime/state.ts"
      to: "src/lib/ipc/runtime.ts"
      via: "snapshot fetch and initialization"
      pattern: "getRuntimeSnapshot|initializeRuntimeState"
    - from: "src/components/status/RuntimeStatusCard.tsx"
      to: "src/features/runtime/state.ts"
      via: "rendered runtime snapshot"
      pattern: "runtimeStatus"
    - from: "src/app/App.tsx"
      to: "src/components/shell/AppShell.tsx"
      via: "shell composition"
      pattern: "AppShell"
---

<objective>
Turn the typed runtime contracts into a minimal but real control-plane UI that satisfies the phase's user-visible success criterion.

Purpose: Complete DESK-02 by making the app navigable and status-driven without leaking future provider/routing implementation into the shell.
Output: App shell, runtime status card, runtime state hook/store, and status-rendering tests.
</objective>

<execution_context>
@/Users/gyuha/workspace/claw-proxy-worktrees/feat-gsd-02/.codex/get-shit-done/workflows/execute-plan.md
@/Users/gyuha/workspace/claw-proxy-worktrees/feat-gsd-02/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-runtime-foundation/01-RESEARCH.md
@.planning/phases/01-runtime-foundation/01-VALIDATION.md
@.planning/phases/01-runtime-foundation/01-02-PLAN.md
@DESIGN.md

<interfaces>
From the runtime contract work:
- `RuntimeSnapshot` contains app readiness, runtime status, profile summary, provider/account slot counts, and last health check.
- UI must consume typed command helpers rather than embedding raw `invoke()` calls in presentation components.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create the runtime state bridge for the UI shell</name>
  <read_first>
    - src/lib/ipc/runtime.ts
    - src/features/runtime/models.ts
    - .planning/phases/01-runtime-foundation/01-RESEARCH.md
  </read_first>
  <files>
    - src/features/runtime/api.ts
    - src/features/runtime/state.ts
    - src/features/runtime/__tests__/runtime-status.test.tsx
  </files>
  <behavior>
    - Test 1: UI runtime state initializes through typed helpers
    - Test 2: Snapshot fields are exposed in a shape components can render without `any`
  </behavior>
  <action>Create `src/features/runtime/api.ts` as the thin adapter over `src/lib/ipc/runtime.ts`, then implement `src/features/runtime/state.ts` to initialize and expose runtime snapshot state for the shell. Add at least one focused test in `src/features/runtime/__tests__/runtime-status.test.tsx` that verifies the runtime state adapter or rendered status behavior using the typed snapshot shape. Keep this state layer narrowly about Phase 1 runtime status, not future provider/routing configuration editing.</action>
  <verify>
    <automated>pnpm vitest run src/features/runtime/__tests__/runtime-status.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    - src/features/runtime/state.ts imports typed runtime helpers
    - src/features/runtime/state.ts does not contain `any`
    - src/features/runtime/__tests__/runtime-status.test.tsx exists
  </acceptance_criteria>
  <done>The shell has a test-backed UI bridge to the runtime snapshot.</done>
</task>

<task type="auto">
  <name>Task 2: Build the app shell and runtime status card</name>
  <read_first>
    - src/app/App.tsx
    - src/features/runtime/state.ts
    - DESIGN.md
    - .planning/phases/01-runtime-foundation/01-RESEARCH.md
  </read_first>
  <files>
    - src/components/shell/AppShell.tsx
    - src/components/status/RuntimeStatusCard.tsx
    - src/app/App.tsx
    - src/styles/app.css
  </files>
  <action>Create `AppShell` and `RuntimeStatusCard` components that render the runtime snapshot in a calm desktop control-plane layout informed by `DESIGN.md`. The shell should include a title area, a status surface driven by real snapshot data, and clear placeholder sections for future areas such as Providers, Routing, and Diagnostics. Update `src/app/App.tsx` to compose the shell and use the runtime state bridge. Use concrete styling tokens or CSS classes; do not leave TODO-only shells or fake status values disconnected from the runtime state layer.</action>
  <verify>
    <automated>pnpm vitest run src/features/runtime/__tests__/runtime-status.test.tsx && rg -n \"Providers|Routing|Diagnostics|Claw Proxy\" src/components/shell/AppShell.tsx src/components/status/RuntimeStatusCard.tsx src/app/App.tsx</automated>
  </verify>
  <acceptance_criteria>
    - src/components/shell/AppShell.tsx exists
    - src/components/status/RuntimeStatusCard.tsx exists
    - src/app/App.tsx renders `AppShell`
    - runtime status component renders from snapshot-driven props or state
  </acceptance_criteria>
  <done>The desktop shell is structured, branded, and driven by runtime-owned data rather than starter placeholders.</done>
</task>

<task type="auto">
  <name>Task 3: Verify end-to-end shell boot for Phase 1 completion</name>
  <read_first>
    - package.json
    - src-tauri/src/lib.rs
    - src/app/App.tsx
    - .planning/phases/01-runtime-foundation/01-VALIDATION.md
  </read_first>
  <files>
    - package.json
    - src/features/runtime/__tests__/runtime-status.test.tsx
  </files>
  <action>Ensure the project exposes a stable command path for Phase 1 verification and that the shell boot path is covered by automated checks where practical. If the current scripts are missing a usable desktop build command, update `package.json` so `pnpm tauri build --debug` or an equivalent script is available. Extend the runtime status test only as needed to cover shell composition and typed snapshot rendering, without turning this phase into a full UI test suite.</action>
  <verify>
    <automated>pnpm vitest run src/features/runtime/__tests__/runtime-status.test.tsx && pnpm tauri build --debug</automated>
  </verify>
  <acceptance_criteria>
    - package.json provides a usable desktop build path
    - `pnpm tauri build --debug` completes successfully
    - the focused runtime status test remains green
  </acceptance_criteria>
  <done>Phase 1 can be verified by both targeted tests and a desktop build smoke check.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| runtime snapshot -> UI shell | Host-owned state crosses into renderer-visible presentation |
| UI state bridge -> command helpers | UI orchestration layer can accidentally re-own runtime truth if not kept thin |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-1-07 | T | `src/features/runtime/state.ts` | mitigate | Keep UI state as a reflection of runtime snapshot, not a competing authority |
| T-1-08 | I | `RuntimeStatusCard` rendering | mitigate | Render only non-secret summary data from the snapshot |
| T-1-09 | D | shell boot verification | mitigate | Keep an automated build smoke check to catch broken desktop boot paths early |
</threat_model>

<verification>
- Run `pnpm vitest run src/features/runtime/__tests__/runtime-status.test.tsx`
- Run `pnpm tauri build --debug`
- Confirm the shell renders Claw Proxy, runtime status, and placeholder future sections
</verification>

<success_criteria>
- User can open Claw Proxy and see a minimal but structured desktop control surface
- The shell renders typed runtime snapshot data from the Rust-side authority
- Phase 1 ends with a repeatable build/test smoke path for future phases
</success_criteria>

<output>
After completion, create `.planning/phases/01-runtime-foundation/01-03-SUMMARY.md`
</output>
